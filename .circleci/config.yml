version 2.1

jobs:
  lint: &lint-unit
    working_directory: ~/circleci-python
    docker:
      - image: "circleci/python:3.6.4"
    steps:
      - checkout
      - run: python --version
      
workflows:
  test:
    jobs:
      -lint 
 
